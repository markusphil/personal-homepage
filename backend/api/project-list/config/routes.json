{
  "routes": [
    {
      "method": "GET",
      "path": "/project-lists",
      "handler": "project-list.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/project-lists/count",
      "handler": "project-list.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/project-lists/:id",
      "handler": "project-list.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/project-lists",
      "handler": "project-list.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/project-lists/:id",
      "handler": "project-list.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/project-lists/:id",
      "handler": "project-list.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
